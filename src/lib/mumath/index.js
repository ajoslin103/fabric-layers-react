/**
 * Composed set of all math utils, wrapped
 *
 * @module  mumath
 */

// Re-export math utilities from fabric-layers core library
export {
  clamp,
  len,
  lerp,
  mod,
  round,
  range,
  order,
  normalize,
  almost,
  lg,
  isMultiple,
  scale,
  pad,
  parseUnit,
  toPx,
  isObj
} from 'fabric-layers';